{
  "name": "red-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute redllamaaa_d1 --local --file=./schema.sql",
    "db:migrate:remote": "wrangler d1 execute redllamaaa_d1 --remote --file=./schema.sql",
    "register": "node register-commands.js"
  },
  "dependencies": {
    "discord.js": "^14.27.0"
  },
  "devDependencies": {
    "wrangler": "^4.122.0"
  }
}
